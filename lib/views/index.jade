DOCTYPE(html)
html(ng-app='yn')
    head(lang="de")
        meta(charset="UTF-8")
        title
            | myTitle
        script(src='//ajax.googleapis.com/ajax/libs/angularjs/1.2.6/angular.min.js')
        script(src='js/code.js', type='text/javascript')
        style.
            li{
                position: relative;
            }
            li span.tagname{
              position: absolute;
              background: white;
              left: 0px;
              width: 100%;
              height: 25px;
              text-align: center;
              bottom: 0px;
              padding-top: 8px;
              display: none;
            }
            li:hover span.tagname{
                display: block;
            }
    body
        h1
            | Data
        div(ng-controller="statsController")
            ul
                li(ng-repeat="user in data.trending_users",style="float:left; style:none;")
                    img(src="http://cdn2.younow.com/php/api/channel/getImage/channelId="+ '{{user.userId}}' +"", title=""+'{{user.username}}'+" "+'{{user.viewers}}')
                    span.tagname {{user.tags[0]}}
            div(style="clear: both;")
            table
                tr(ng-repeat="tag in data.trending_tags")
                    td {{tag.tag}}
                    td(style="text-align: left;") {{tag.score}}